# Wimm Backend - Environment Variables (Windows)

# Database Configuration - SQLite para Windows
DATABASE_URL="file:./prisma/dev.db"

# JWT Configuration
JWT_SECRET=wimm-super-secret-jwt-key-for-development-2024
JWT_EXPIRES_IN=7d

# Application Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration - Ajuste o IP para o seu IP local
CORS_ORIGIN=http://localhost:8081,http://localhost:3000,exp://SEU_IP_LOCAL:8081
